language: python

python:
  - "3.7"

services:
  - docker

branches:
  only:
    - master

install:
  - pip install pipenv
  - pipenv install
  - cd frontend/
  - npm install jest@24.9.0
  - cd ../

script:
  - make test_backend
  - make test_frontend
  - make build
  - make status